@model Wahama.Product;

<form asp-action="Catalogue">
    <div class="form-group">
        <select asp-for="ProductFraction" class="form-control" asp-items="@ViewBag.ProductFraction"></select>
    </div>
    <div class="form-group">
        <input type="submit" value="Save" class="btn btn-default" />
    </div>

</form>